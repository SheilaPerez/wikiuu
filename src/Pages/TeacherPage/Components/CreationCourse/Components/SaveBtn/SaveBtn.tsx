import styles from './SaveBtn.module.css';

const SaveBtn = () => {
    return (
        <div>
            <button type="button" className={styles.saveBtn}>Guardar</button>
        </div>
    )
}

export default SaveBtn;